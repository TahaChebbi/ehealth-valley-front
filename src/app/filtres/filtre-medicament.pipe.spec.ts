import { FiltreMedicamentPipe } from './filtre-medicament.pipe';

describe('FiltreMedicamentPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreMedicamentPipe();
    expect(pipe).toBeTruthy();
  });
});
